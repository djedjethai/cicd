name: deploy-kvs

on:
  push: 
    branches:
      - master
    paths:
      - "./**"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: action/checkout@v2
      - run: docker build -t djedjethai/kvs .
